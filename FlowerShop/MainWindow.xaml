<Window x:Class="FlowerShop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlowerShop"
         xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1080" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" WindowStyle="None" Background="LightYellow" Loaded="Window_Loaded">
    <Grid>
        <Image Source="resource/Flower.jpg" Margin="-4,0,847,506" />
        <TextBlock HorizontalAlignment="Left" Margin="137,70,0,0" TextWrapping="Wrap" VerticalAlignment="Top"/>
        <TextBox x:Name="txtSearch" Width="313" HorizontalAlignment="Left" VerticalAlignment="Top"  Margin="101,146,0,0" Height="42"   />
        <Label Content="Search" HorizontalAlignment="Left" Margin="33,149,0,0" VerticalAlignment="Top"/>
        <Button x:Name="btnSearch" Content="Search" HorizontalAlignment="Left" Margin="440,141,0,0" VerticalAlignment="Top" Height="47" Width="73"  Background="#FF2CD91C" Click="btnSearch_Click"/>
        <Button x:Name="btnAdd" Content="Add" HorizontalAlignment="Left" Margin="570,144,0,0" VerticalAlignment="Top" Height="46" Width="73"  Background="#FF2CD91C" Click="btnAdd_Click"/>
        <Button x:Name="btnDelete" Content="Delete" HorizontalAlignment="Left" Margin="690,141,0,0" VerticalAlignment="Top" Height="47" Width="73" Background="#FF2CD91C" Click="btnDelete_Click"/>
        <Button x:Name="btnClose" Content="Close" HorizontalAlignment="Left" Margin="809,139,0,0" VerticalAlignment="Top" Height="47" Width="73"  Background="#FF2CD91C" Click="btnClose_Click"/>
        <DataGrid x:Name="PostFlowerDataGrid" HorizontalAlignment="Left" Height="312" Margin="33,232,0,0" VerticalAlignment="Top" Width="868" AutoGenerateColumns="False" TabIndex="7"  >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id"  Binding="{Binding Id}" />
                <DataGridTextColumn Header="Name"  Binding="{Binding Name}" />
                <DataGridTextColumn Header="Description" Width="100"  Binding="{Binding Description}" />
                <DataGridTextColumn Header="Thumbnail"  Binding="{Binding Thumbnail}" />
                <DataGridTextColumn Header="Address"  Binding="{Binding Address}" />
                <DataGridTextColumn Header="StartDate"  Binding="{Binding StartDate}" />
                <DataGridTextColumn Header="EndDate"  Binding="{Binding EndDate}" />
                <DataGridTextColumn Header="Price"  Binding="{Binding Price}" />
                <DataGridTextColumn Header="UserId"  Binding="{Binding UserId}" />
                <DataGridTextColumn Header="Category"  Binding="{Binding Category.Name}" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>

</Window>
